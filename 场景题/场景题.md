

### 秒杀

1. 前端提交按钮限频/添加验证码校验（1是把时间岔开2是防止机器攻击）
2. 网关限流
	Nginx
	Gateway 的 RequestRateLimiter 
	Sentinel 熔断降级

![[deepseek_mermaid_20260826_6b91c7.png]]
3. 资源动静分离,用CDN缓存静态资源
4. Redis + Lua 把把“检查库存、扣减库存、记录用户”三个动作打包成一个整体执行
5. MQ削峰填谷 异步化下单 保证最终一致性
6. 数据库乐观锁
	UPDATE seckill_product SET stock = stock - 1 WHERE id = #{productId} AND stock >= 1; -- 核心兜底条件
	
	如果影响行数为0，说明数据异常，触发补偿逻辑（如回补Redis库存）